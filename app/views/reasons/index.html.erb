<h1>Need a reason to drink?</h1>
<p>Keep track of all of your reasons to drink and access them when needed:</p>
<p>Enter a new reason:</p>
<form action= "/reasons" method="post">
  <label for = "description">Description</label>
  <input id ="description" name = "description" type = "text">
  <button>Add Reason!</button>
</form>

<h2>Reasons</h2>

<ul>
  <% @reasons.each do |reason| %>
    <li><%= reason.description %> <a href ="/reasons/<%= reason.id %>" data-confrim="Are you sure you don't want to drink it again?" data-method = "delete" rel="nofollow">Bad reason!</a>
    <form action="/reasons/<%= reason.id %>" method="post">
      <input name="_method" type="hidden" value="patch">
      <input name="reason[done]" type="hidden" value="true">
      <button>Mark DRANK</button>
    </form></li>
  <% end %>
</ul>


